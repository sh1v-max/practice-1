// Task 3: Write a variable `id: string | number`. Assign both a string and a number to it
// at different points, and print its type each time using `typeof`.

let id: string | number;

id = "A101";
console.log(`id = ${id}, type = ${typeof id}`);

id = 101;
console.log(`id = ${id}, type = ${typeof id}`);
